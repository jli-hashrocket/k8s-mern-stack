<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Home</title>
	</head>
	<body>
		<h1>Home Page</h1>
		<main>
			<div><a href="/add-account">Add Account</a></div>
			<div>
				<% accounts.forEach(account => { %>
					<li><a href="/<%= account.id %>"><%= account.last_name %>, <%= account.first_name %></a> <a href="/edit/<%= account.id %>">Edit</a></li>
				<% }) %>
			</div>
		</main>
	</body>
</html>
